apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "vikunja-frontend.fullname" . }}-test-connection"
  labels:
    {{- include "vikunja-frontend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "vikunja-frontend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
